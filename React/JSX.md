# JSX
- O JSX permite com que seja possível escrever código HTML dentro da sintaxe do JavaScript
- Dessa forma não é necessário criar vários arquivos para termos um componente funcional.
- Exemplo:
```ts
return <h1>Hello world</h1>
```
